model Feedback {
    id         String   @id @default(uuid())
    content    String
    review     Int
    customerId String
    mealId     String
    meal       Meals    @relation(fields: [mealId], references: [id])

    @@index([customerId])
    @@index([mealId])
}
